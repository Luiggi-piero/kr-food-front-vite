import axios from 'axios';

axios.defaults.baseURL =
    // 'http://localhost:5000'
    'https://kr-food-backend.onrender.com'
    // import.meta.env.NODE_ENV !== 'production' ? 'http://localhost:5000' : 'https://kr-food-backend.onrender.com';
    // import.meta.env.NODE_ENV !== 'production' ? 'http://localhost:5000' : '/';